'use client';

import { useUrlControlledModal } from 'Hooks/use-url-controlled-modal';

const ModalRedirectHandler = () => {
	const _ = useUrlControlledModal();
	return null;
};

export default ModalRedirectHandler;
