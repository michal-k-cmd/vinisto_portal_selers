// currently there's nothing here - StockingRequests were misplaced,
// I'll keep the structure for future use,
// base URI shouldn't be exported, but ts modules demand an export.

export const WAREHOUSE_SERVICE_BASE_URI = 'warehouse-api';
