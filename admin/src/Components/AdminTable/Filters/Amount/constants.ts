export const AMOUNT_FILTER_DELIMITER = ':';

export enum AMOUNT_FILTER_KEYS {
	LESS = 'LESS',
	EQUALS = 'EQUALS',
	GREATER = 'GREATER',
}

export enum AMOUNT_FILTER {
	LESS = '<',
	EQUALS = '=',
	GREATER = '>',
}

export const PERCENTAGE_MIN = 0;
export const PERCENTAGE_MAX = 100;
